<%= simple_form_for([@group, @motion]) do |f| %>
  <%= f.error_notification %>
  <%= f.input :name %>
  <%= f.input :text, input_html: { cols: 80, rows: 20 } %>
  <%= f.button :submit, class: 'btn-primary' %>
<% end %>
